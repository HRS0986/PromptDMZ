"""C10 — FastAPI demonstration server (Phase 9).

    app.py      load model + adapters + artefacts ONCE at startup; POST /detect
    schemas.py  DetectRequest / DetectResponse pydantic models

Requires the `serve` extra: `uv sync --extra serve`.
"""
